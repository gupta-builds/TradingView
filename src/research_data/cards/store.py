"""RESERVED — DuckDB ``evidence_cards`` table (B2 build #2).

Do not implement until golden Properties pass and the live A3 NVDA card
needs no breaking field change. Mirror ``raw_market_payloads`` hash+path
shape: card_id PK, card_path, symbol, as_of, schema_version, nullable spec_id.
"""

from __future__ import annotations

# Intentionally empty — build #2 destination.
