# North star — research desk (out of Phase 3 V1)

> Vision capture so it is not lost. **Nothing here is Phase 3 V1 scope.**

## Deferred product surfaces

| Item | Why deferred |
|---|---|
| LLM StrategySpec proposer (V1.1+) | After card quality eval (golden Properties + live blind compare) |
| Research-proposer swarm (vault/papers → Citations + PROPOSED specs) | Feeds human-gated loop only; never confidence/gates/promote |
| Tutor (beginner pass over finalized EvidenceCard) | Decision-inert; V1 or V1.1 |
| DuckDB `evidence_cards` table (build #2) | After live NVDA card stable — no migration story |
| Brier / calibration math | Needs a **named binary event** + predicted probability; action/confidence are not that yet (e.g. “outperform VOO over horizon H?” undecided) |
| Streamlit / polished UI | desk-is-real ≠ UI |
| Sibling PM / Kalshi / Polymarket app | Parked per 2026-06-25 postmortem until stocks/ETFs paper trading is live, tested, real-use ready |
| Kronos RankIC + inference | Reserved schema only until validation session |
| Azure OpenAI higher volume | Free-tier Gemini/Groq sufficient for V1; Azure held in reserve |
| Nightly GHA live LLM | After volume justifies |

## Standing law (never “later soften”)

Four gates in order; human `anant` approves; AI never promotes; no execution language; Python owns facts.
